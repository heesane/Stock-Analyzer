# Database Layer

This directory stores database-related assets (migrations, sample data, or scripts). SQLite databases created during local development will be ignored via `.gitignore`. Future milestones will add migration tooling for MySQL/PostgreSQL.
